{
  "name": "mx_enterprise-web",
  "version": "1.0.0",
  "description": "Web interface for mx_enterprise",
  "scripts": {
    "dev": "cross-env NODE_ENV=development node ../scripts/update-fxmanifest.js && vite",
    "build": "cross-env NODE_ENV=production node ../scripts/update-fxmanifest.js && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.16",
    "cross-env": "^7.0.3",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0",
    "vite": "^5.0.8"
  }
}
